//static memory allocarion
// int x = 5;
//size and alocation where variable will be stored is fixing during cmpile time
//location wherevariable will be stored is  saved, but the actual physical memory is not allocated to variable a compile time.
//allocation will be at runtime only
//static memory allocation in slightly faster than dynamic memory allocation 
// allocation and deallocation is done by copier itself 